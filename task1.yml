---
platform: linux

outputs:
- name: output-1-1

image_resource:
  type: docker-image
  source: {repository: alpine, tag: latest}

run:
  path: /bin/bash
  args: 
  - "date; hostname; echo hello; date>output-1-1/date"
